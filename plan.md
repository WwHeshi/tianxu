# 天序八字分析系统实施计划

- 更新日期：2026-08-08
- 当前阶段：报告式 MVP
- 详细的后续知识库方案：`docs/agent-knowledge-base-plan.md`

## 1. 当前产品决策

第一版只完成一条短而可验证的链路：

```text
输入出生信息
  -> 服务端确定性排盘
  -> 页面展示命盘
  -> 用户在页面底部点击“生成分析报告”
  -> 服务端重新排盘并精简上下文
  -> 一次模型调用
  -> 展示固定结构报告
```

第一版明确不做：

- 对话和连续追问。
- 知识库、RAG、全文检索或古籍引用。
- Agent 工具和 function calling。
- 多 Agent 编排。
- 出生资料、命盘和报告历史记录。
- 用户登录、付费、分享和报告导出。

这样可以先验证排盘到报告的完整体验、模型输出稳定性和用户是否愿意阅读报告，再决定知识库与 Agent 的复杂度。

## 2. 架构边界

### 2.1 排盘引擎

排盘引擎是唯一可以计算命盘的模块。它负责：

- 公历/农历输入校验。
- 北京时间与真太阳时换算。
- 四柱、藏干、十神、五行、神煞。
- 大运、流年和流月。
- 计算规则及引擎版本。

模型不得重新计算、修改或补造任何命盘字段。

### 2.2 报告服务

报告服务不是自由式 Agent。它只做以下工作：

1. 接收与排盘接口相同的 `BirthInput`，不接收客户端构造的命盘 JSON。
2. 在服务端重新排盘。
3. 提取四柱和派生数据，并只选择当前大运、当前流年、当前流月。
4. 调用一次模型，不传工具，不传聊天历史。
5. 用 Pydantic 校验严格 JSON 输出。
6. 返回服务端命盘、报告和模型/提示词/Schema 版本。

### 2.3 报告固定章节

```text
命盘概览
性情特点
能力与事业
财务倾向
关系模式
当前运势周期
综合建议
局限说明
```

报告使用审慎、概率性的表达，并明确属于传统文化视角下的参考。不得虚构书籍或引文，不作疾病诊断、寿命判断、灾祸断言，也不给出确定性的医疗、法律或投资建议。

## 3. 当前技术结构

```text
Next.js 页面
  |-- 出生信息表单
  |-- 命盘结果
  |-- 右上角模型 API 设置
  `-- 页面底部报告生成与展示
          |
          | REST
          v
FastAPI
  |-- 确定性排盘引擎
  |-- 模型设置 API
  |-- 报告上下文构建
  |-- Responses / Chat Completions 协议适配
  `-- Pydantic 输出校验
          |
          v
PostgreSQL
  `-- 加密后的模型凭据
```

第一版不需要 Redis、任务队列、向量数据库、SSE 或微服务。

## 4. 模型设置与密钥安全

页面右上角提供模型 API 设置。数据库表为未来用户系统预留 `user_id`，当前匿名本地版本使用固定的 `local-default` scope。

数据库只保存：

- provider。
- API 协议（Responses 或 Chat Completions）。
- model。
- base URL。
- API 密钥密文。
- API 密钥末四位。
- 主密钥版本。
- 创建与更新时间。

服务端环境变量保存 `APP_ENCRYPTION_KEY`。API 密钥使用 AES-GCM 加密，主密钥永远不进入数据库；明文 API 密钥不会回传前端、写入日志或放进 `NEXT_PUBLIC_*` 环境变量。

设置界面只显示是否已配置和掩码，不回填原密钥。更换密钥时必须重新输入完整新密钥。

当前没有认证，模型设置和报告能力只能在 `development`、`local`、`test` 环境开启；生产环境代码会强制关闭，待用户系统和授权完成后再开放。

## 5. API

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `GET` | `/api/v1/health` | 服务健康检查 |
| `POST` | `/api/v1/charts/preview` | 服务端排盘 |
| `GET` | `/api/v1/model-settings` | 读取脱敏后的模型配置状态 |
| `PUT` | `/api/v1/model-settings` | 加密保存完整新配置 |
| `POST` | `/api/v1/model-settings/test` | 测试当前模型连接，不保存表单 |
| `DELETE` | `/api/v1/model-settings` | 清除模型配置 |
| `POST` | `/api/v1/reports/generate` | 重新排盘并生成一次性报告 |

报告接口失败时返回可恢复错误：未配置密钥、鉴权失败、限流、网络错误、超时和输出 Schema 不合格都分别提示，但不向客户端暴露上游响应体或密钥。

## 6. 实施阶段

### 阶段 A：报告式 MVP（当前）

- PostgreSQL、SQLAlchemy 和 Alembic。
- 模型凭据加密存储。
- 模型设置 API 和右上角设置弹窗。
- 精简报告上下文。
- 一次性结构化模型调用。
- 页面底部报告入口和八章节展示。
- 报告完成后的可视化执行链路、提示词和脱敏请求快照。
- 后端单元/API 测试、前端 lint/build、Docker 启动验证。

验收条件：用户能完成排盘、配置模型并生成报告；数据库中不存在明文密钥；模型不会收到完整大运时间线或工具定义。

### 阶段 B：用户系统与生产安全

- 用户注册、登录、会话和授权。
- 将凭据归属从 `local-default` 切换到 `user_id`。
- 写接口 CSRF/来源保护与请求限流。
- 主密钥轮换与旧版本解密策略。
- 审计事件、日志脱敏、删除能力。
- 生产数据库备份和监控。

验收条件：任何用户只能管理和使用自己的模型凭据，匿名请求无法消耗模型额度。

### 阶段 C：知识库基础（后续可选）

先做可验证的 PostgreSQL 全文/字符级检索，不做纯向量 RAG：

- 一级原文和二级注解分表或明确分级。
- 书名、版本、卷、章节、页码和版权状态。
- 原文、规范化文本、繁简文本和校对状态。
- 精确短语、字符 n-gram、元数据过滤和术语扩展。
- 检索评测集和引用真实性校验。

只有关键词方案在人工评测中出现稳定的语义召回缺口，才实验向量召回，并且只作为混合检索的一路。

### 阶段 D：Agent 与对话（后续可选）

知识库稳定后，先使用单个解读 Agent 加普通检索模块。只有检索规划明显复杂、一次召回经常不足或检索和写作互相干扰时，才拆成“资料研究 Agent + 命理解读 Agent”。二者只交换结构化证据包，并限制补充检索次数。

## 7. 测试重点

- 相同输入的排盘结果保持确定。
- 报告接口忽略客户端命盘，只使用服务端重算结果。
- 模型上下文中不存在完整 `big_luck_periods`。
- 模型请求只有一次，没有 tools/function calling。
- 密钥加密可往返，篡改密文会失败。
- GET 设置接口只返回掩码。
- 生产环境无法强行开启匿名模型设置和报告。
- 模型超时、401/403、429、非 2xx 和非法 JSON 都能安全失败。
- 桌面和移动布局均能在命盘最底部找到报告按钮。

## 8. 本阶段完成定义

- 后端迁移可在全新 PostgreSQL 上执行。
- 右上角可以保存、查看掩码和清除模型设置。
- 页面底部可以生成、重试并展示八章节报告。
- 报告元数据包含模型、提示词版本、Schema 版本和排盘引擎版本。
- API 密钥不以明文落库、不回传、不记录日志。
- 后端测试与 Ruff 通过，前端 ESLint 与生产构建通过。
- Docker Compose 可以自动执行迁移并健康启动。
