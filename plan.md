# 八字分析 Agent 项目计划

## 1. 项目目标

构建一个包含前端和后端的八字分析 Web 应用。用户输入出生信息后，系统先通过确定性的排盘引擎生成命盘，再由 AI Agent 基于命盘数据生成分析报告并回答后续问题。

第一版以一条完整、可验证的用户链路为目标：

```text
出生信息 -> 排盘 -> 命盘展示 -> AI 分析报告 -> 连续追问
```

项目定位为传统文化与娱乐类产品。对于健康、财务、法律等高风险问题，不输出确定性结论，也不替代专业意见。

## 2. 核心原则

1. **排盘与解释分离**：排盘引擎负责计算，AI Agent 负责解释，禁止让模型自行推算四柱。
2. **结果可复现**：同一输入、同一计算策略和同一引擎版本必须得到相同命盘。
3. **规则可追踪**：节气边界、换日方式、时间基准等规则必须版本化并展示给用户。
4. **输出结构化**：命盘数据和 AI 报告均使用固定的数据结构，避免依赖不可控的自由文本。
5. **先完成垂直切片**：先交付基础排盘、报告和追问，再扩展流年、合婚、择日等功能。
6. **隐私优先**：出生时间和地点属于敏感个人信息，不写入普通日志，并提供删除能力。

## 3. MVP 范围

### 3.1 包含功能

- 支持按北京时间输入公历出生日期和时间。
- 支持中国大陆、香港、澳门、台湾的版本化静态地点选择和性别输入。
- 根据末级地点代表点经度和均时差换算真太阳时。
- 生成年柱、月柱、日柱、时柱。
- 展示天干地支、藏干、十神、五行分布等基础信息。
- 展示本次排盘采用的计算规则和引擎版本。
- 生成结构化的基础分析报告。
- 支持围绕当前命盘连续追问，并流式返回回答。
- 支持基本的异常提示、重新排盘和会话清空。
- 提供桌面端和移动端响应式界面。

### 3.2 暂不包含

- 支付、会员和订单系统。
- 原生 iOS/Android 应用。
- 多智能体协作。
- 多个命理流派同时分析。
- 合婚、择日、姓名学等扩展模块。
- 自动抓取互联网命理内容。
- 复杂运营后台和内容社区。

大运、流年、用户登录及历史记录是否进入 MVP，需在开发前确认。

## 4. 用户流程

1. 用户打开应用，直接进入出生信息表单。
2. 前端校验北京时间、静态地点 ID 和性别。
3. 后端查询地点独立代表点经度，将北京时间换算为真太阳时，并记录本次计算策略。
4. 排盘引擎生成结构化命盘数据。
5. 前端先展示可验证的命盘结果。
6. 用户发起分析，Agent 读取命盘和规则库生成报告。
7. 用户针对报告继续提问，Agent 始终引用同一份命盘上下文。
8. 用户可以重新排盘、清空会话或删除已保存数据。

## 5. 技术架构

```text
Next.js Web
    |
    | REST / SSE
    v
FastAPI Backend
    |-- Input Normalizer
    |-- Bazi Engine
    |-- Agent Orchestrator
    |-- Model Provider Adapter
    |-- Persistence Layer
    v
PostgreSQL
```

### 5.1 推荐技术栈

| 模块 | 推荐方案 | 用途 |
| --- | --- | --- |
| 前端 | Next.js + TypeScript | 页面、表单、命盘展示和对话界面 |
| 样式 | Tailwind CSS + 轻量组件库 | 响应式布局和统一交互 |
| 后端 | FastAPI + Python | API、排盘编排、模型调用和数据访问 |
| 数据校验 | Pydantic | 输入、命盘和 Agent 输出结构校验 |
| 数据库 | PostgreSQL | 命盘、报告、会话和消息持久化 |
| 数据迁移 | Alembic | 数据库版本管理 |
| AI 接入 | Provider Adapter | 隔离具体模型厂商，便于切换 |
| 流式响应 | Server-Sent Events | 分析报告和对话逐步返回 |
| 本地环境 | Docker Compose | 统一启动前端、后端和数据库 |
| API 契约 | OpenAPI 生成 TypeScript 类型 | 保持前后端数据结构一致 |

MVP 阶段不强制引入 Redis、消息队列或微服务。只有在出现长任务、并发和限流需求后再增加。

### 5.2 建议目录结构

```text
tianxu/
  frontend/              # Next.js 前端
  backend/               # FastAPI 后端
    app/
      api/               # HTTP/SSE 接口
      bazi/              # 排盘领域逻辑
      agent/             # Agent 编排与提示词
      models/            # 数据库模型
      schemas/           # Pydantic 数据结构
      services/          # 应用服务
    tests/
  knowledge/             # 版本化命理规则库
  docs/                  # ADR、接口和产品文档
  infra/                 # Docker 和部署配置
  plan.md
```

## 6. 排盘引擎设计

排盘引擎是后端中的确定性计算模块，不是 AI 模型。

### 6.1 输入

- 北京时间字段 `beijing_datetime`。
- 输入历法类型，MVP 默认公历。
- 静态地点标识 `location_id`；地点名称、真实层级、时区和坐标均由后端快照解析。
- 性别，大运规则需要时使用。
- `calculation_policy`，即本次排盘规则。

客户端不提交经度、名称、层级或时区。大陆地点 ID 以六位正式末级行政区代码构造；
港澳台使用带地区命名空间的当地官方代码，或与明确来源身份绑定的项目稳定 ID，避免
伪造大陆六位代码。后端查询版本化静态地点表并返回规范化的可变长度
`division_path`。直辖市区县、省直辖县级单位、香港和澳门均不人为补造中间层。

### 6.2 输出

- 标准化后的北京时间、规范化地点信息和换算后的真太阳时。
- 地点代表点经度、东经 120° 标准经线、经度修正、均时差、总修正和坐标匹配方式。
- 年柱、月柱、日柱和时柱。
- 天干、地支、阴阳和五行。
- 藏干、十神、纳音等派生信息。
- 可选的大运方向、起运时间和大运列表。
- 计算策略、引擎版本和输入摘要。
- 警告信息，例如出生时间不确定或位于规则边界附近。

### 6.3 初始计算策略

以下为 v1 已确定的默认口径：

| 规则 | v1 默认值 |
| --- | --- |
| 年柱边界 | 立春 |
| 月柱边界 | 节气 |
| 换日时间 | 真太阳时 00:00 |
| 输入时间基准 | 北京时间 |
| 排盘时间基准 | 真太阳时，固定启用 |
| 大运规则 | 选择流派后固定并版本化 |

真太阳时按以下公式计算：

```text
真太阳时 = 北京时间 + 4 ×（地点代表点经度 - 120°）分钟 + 均时差
```

大陆经度取正式末级行政单位中心，香港取各区官方民政咨询中心，澳门取 GeoNames 地理
区域代表点，台湾取官方派生区域边界质心。这些点都只代表区级近似值而非具体出生地址。同一区域内实际
经度可能造成数分钟偏差；出生时间临近换日、时辰或节气边界时，结果必须带有复核提示。

### 6.4 实现策略

- 评估 `lunar-python` 与 `sxtwl` 的能力和边界行为。
- 在第三方库外增加自有适配层，业务代码不直接依赖库的数据结构。
- 不盲信第三方库结果，以已知命盘样例进行交叉校验。
- 大陆行政区主数据锁定民政部国家地名信息库快照，只保留正式末级行政单位；功能区、园区、统计管理区和虚拟层级不得进入用户选择器。
- 维护版本化的地点代表点经度表，记录数据来源、地点代码版本、匹配方式和坐标精度。
- 末级行政单位没有经过核验的独立坐标时必须停止排盘，禁止使用所属城市、地区或省级中心点回退。
- 香港接入民政事务总署 18 区，澳门接入七个传统堂区和路氹城共 8 个地理区域，台湾
  接入 368 个官方乡镇市区；使用命名空间 ID、保留当地 IANA 时区元数据，不从非权威
  大陆统计包补全，也不把澳门传统堂区描述为现行正式行政区。
- 将经度修正与均时差计算封装为独立、可测试的真太阳时模块。
- 将边界规则封装为显式配置，禁止散落在业务代码中。
- 每次保存命盘时同时保存 `engine_version` 和 `policy_version`。

## 7. Agent 设计

### 7.1 Agent 输入

Agent 只接收以下经过验证的数据：

- 排盘引擎输出的命盘 JSON。
- 本次计算策略和异常提示。
- 版本化命理规则库中的相关内容。
- 用户当前问题和必要的对话历史。

### 7.2 Agent 输出

报告采用结构化输出，建议至少包含：

```text
summary                 总览
calculation_basis       分析所依据的命盘信息
element_analysis        五行与日主分析
interpretations         分主题解读
cautions                不确定性和注意事项
follow_up_questions     建议追问方向
```

所有结构化输出必须经过 Pydantic 校验。校验失败时进行有限次数重试，仍失败则返回明确的可恢复错误。

### 7.3 Agent 约束

- 不允许重新计算或修改四柱。
- 重要判断需要说明依据了哪些命盘字段。
- 对流派存在分歧的内容明确标注假设。
- 避免绝对化预测、恐吓性措辞和高风险决策建议。
- 提示词、规则库和模型参数均记录版本，便于回归测试。
- MVP 使用单个编排器，不引入多智能体。

## 8. 前端页面

### 8.1 排盘页

- 出生日期和北京时间输入。
- 中国大陆、香港、澳门、台湾真实可变层级联动选择；中间层不存在时直接选择末级地点。
- 性别选择。
- 高级规则入口，MVP 可默认折叠。
- 输入不完整、时间不确定和边界情况提示。

### 8.2 命盘结果页

- 四柱作为首要视觉信息。
- 展示十神、藏干和五行分布。
- 展示北京时间、真太阳时、经度修正、均时差、地点精度和引擎版本。
- 提供“开始分析”和“重新排盘”操作。

### 8.3 分析与对话页

- 分主题展示分析报告。
- 保留当前命盘摘要，避免对话中上下文混淆。
- 支持流式回答、停止生成、重新生成和错误重试。
- 提供清空会话操作。

### 8.4 历史记录页

仅在 MVP 确认需要持久化和账号体系后实现。

## 9. API 草案

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| `POST` | `/api/v1/charts/preview` | 校验输入并生成临时命盘 |
| `POST` | `/api/v1/charts` | 保存出生资料和命盘 |
| `GET` | `/api/v1/charts/{chart_id}` | 获取命盘详情 |
| `DELETE` | `/api/v1/charts/{chart_id}` | 删除命盘和关联数据 |
| `POST` | `/api/v1/charts/{chart_id}/analyses` | 生成结构化分析报告 |
| `POST` | `/api/v1/conversations` | 创建命盘关联会话 |
| `POST` | `/api/v1/conversations/{id}/messages` | 发送消息并通过 SSE 返回回答 |
| `GET` | `/api/v1/conversations/{id}` | 获取对话历史 |
| `GET` | `/api/v1/calculation-policies` | 获取可用排盘规则 |

接口路径和是否保存数据将在确认匿名模式或账号模式后调整。

## 10. 初始数据模型

| 实体 | 关键字段 |
| --- | --- |
| `users` | 用户标识、创建时间、删除时间；匿名模式可暂不创建 |
| `birth_profiles` | 原始北京时间、地点 ID、规范化地点路径、真太阳时换算摘要、用户标识 |
| `charts` | 命盘 JSON、输入摘要、引擎版本、规则版本 |
| `analyses` | 报告 JSON、模型、提示词版本、规则库版本 |
| `conversations` | 关联命盘、状态、创建时间 |
| `messages` | 角色、内容、模型元数据、创建时间 |
| `feedback` | 评分、问题类型、用户备注 |

## 11. 开发阶段

### 阶段 0：产品和规则确认

交付物：

- MVP 功能清单。
- 默认排盘规则说明。
- 命盘 JSON Schema。
- 20 至 50 个已知结果的 golden test 样例。
- 模型供应商、部署地区和数据保存策略决定。

验收条件：团队能对同一出生案例明确说出输入、计算口径和期望输出。

### 阶段 1：项目骨架

交付物：

- Next.js 和 FastAPI 基础项目。
- PostgreSQL 与数据库迁移。
- Docker Compose 本地环境。
- 代码格式化、静态检查和测试命令。
- 前后端健康检查与基础 CI。

验收条件：新环境可以按文档启动全部服务，并完成一次前后端请求。

### 阶段 2：排盘内核

交付物：

- 出生信息标准化。
- 排盘库适配层。
- 四柱及基础派生数据。
- 计算策略与版本管理。
- 排盘 API 和单元测试。

验收条件：golden tests 全部通过，重复请求结果稳定一致。

### 阶段 3：前端垂直切片

交付物：

- 出生信息表单。
- 命盘结果页。
- 计算规则和异常提示。
- 移动端与桌面端适配。

验收条件：用户可以从空白页面完成输入并看到准确命盘。

### 阶段 4：Agent 和对话

交付物：

- 模型 Provider Adapter。
- 版本化系统提示词和命理规则库。
- 结构化分析报告。
- SSE 流式对话。
- 错误重试、超时、停止生成和基本安全约束。

验收条件：模型不修改命盘数据，报告结构稳定，追问保持同一命盘上下文。

### 阶段 5：数据、隐私和部署

交付物：

- 根据产品决定加入匿名会话或账号体系。
- 数据删除、日志脱敏和请求限流。
- 生产环境配置、数据库备份和基础监控。
- 部署文档和故障处理说明。

验收条件：可在目标环境部署，用户可删除自己的数据，日志不包含完整出生资料。

### 阶段 6：后续扩展

- 大运和流年深度分析。
- 多流派和具体地址或实际坐标的高精度地点模式。
- 合婚、择日等独立模块。
- PDF 或图片报告导出。
- 账号、订阅和支付。
- 运营后台、反馈闭环和分析质量评测平台。

## 12. 测试计划

### 12.1 排盘引擎测试

- 立春前后和各月节气交界。
- 真太阳时 23:00、00:00 与子时边界。
- 闰年、月底、年末和闰月转换。
- 中国东西部典型区县、东经 120° 两侧和修正后跨日期情况。
- 公历与农历转换。
- 正式行政代码查表、每度 4 分钟经度修正和不同季节的均时差。
- 直辖市、省直辖县、无县级辖区城市、失效代码、伪造层级和边界风险提示。
- 相同输入在相同版本下的确定性。

### 12.2 应用测试

- API Schema 和前后端契约测试。
- 表单校验与错误状态测试。
- SSE 中断、重试和超时测试。
- 数据保存与级联删除测试。
- 桌面端和移动端关键流程测试。

### 12.3 Agent 评测

- 是否引用了正确命盘字段。
- 是否出现自行改盘或虚构字段。
- 相同命盘报告结构是否稳定。
- 边界与不确定信息是否得到提示。
- 高风险问题是否避免确定性建议。
- 不同模型和提示词版本的回归对比。

## 13. 隐私与安全

- 不在访问日志和错误日志中记录完整出生日期、时间和地点。
- 模型调用只发送完成任务所需的最小数据。
- 数据库存储、备份和传输使用部署平台提供的加密能力。
- 用户能够删除命盘、报告和对话。
- 所有写接口进行输入校验、大小限制和速率限制。
- 对模型提示词注入和越权读取进行测试。
- 页面明确说明产品定位、计算口径及输出局限。

## 14. MVP 完成定义

满足以下条件才视为 MVP 完成：

- 用户可以完成出生信息输入、排盘、报告生成和连续追问。
- 排盘引擎通过约定的全部 golden tests。
- 页面明确展示北京时间、真太阳时换算明细、换日规则和引擎版本。
- Agent 不承担历法计算，不会修改后端给出的命盘。
- 核心流程在常见手机和桌面尺寸下可正常使用。
- 模型异常、网络中断和无效输入都有可恢复的界面状态。
- 敏感数据不进入普通日志，并具备删除路径。
- 项目可以通过文档在新环境中启动和部署。

## 15. 开发前待确认事项

1. 产品面向个人使用，还是公开 SaaS？
2. MVP 是否包含大运和流年？
3. 是否支持农历输入？
4. 目标部署地区及可用的模型供应商是什么？
5. 第一版是否需要登录、跨设备历史记录和数据持久化？
6. 分析风格偏传统命理术语，还是偏现代易读表达？
7. 是否需要报告分享或导出？
8. 哪些分析主题可以进入第一版，例如事业、关系、健康、财运？

完成以上决策后，先冻结阶段 0 的规则与数据契约，再开始项目骨架和排盘内核开发。
